<div class="col-md-offset-3 col-md-6">

<table class="table">
  <thead>
    <tr><td><%= t('users.show.title') %></td></tr>
  </thead>
  <tr>
    <td><%= t('users.show.name') %></td>
    <td><%= "#{@user.fname} #{@user.lname}" %></td>
  </tr>
  <tr>
    <td><%= t('users.show.email') %></td>
    <td><%= @user.email %></td>
  </tr>
  <tr>
    <td><%= t('users.show.gender') %></td>
    <td><%= @user.gender == 'M' ? t('users.show.gender_male') : t('users.show.gender_female') %></td>
  </tr>
  <tr>
    <td><%= t('users.show.age') %></td>
    <%# TODO: bug. leap years not taken into count %>
    <td><%= ((Date.today - @user.dob) / 365).to_i %></td>
  </tr>
</table>

</div>